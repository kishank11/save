Q1


e)  update Employee set salary = 50000
where Employee_no = 2:

b)  select name from Employee
where salary » 50000;

c)   alter table Department add Employee_no INT; alter table Department
add foreign key (Employee_no) references Employee(EmpIoyee_no);

d)  ALTER TABLE Employee
ALTER address SET DEFAULT ’Mumbai';

e)  select avg(salary) from Employee;


Q2)

o)  select Employee_name from Employee where Employee_name like ’%s%’;

b)  select Employee_name, job from Employee where Dept_no in (10,20);

c)  selec† Employee_name, job from Employee order by Employee_name asc:

d)  SELECT Dept_no, COUNT(*) as no_of_emp FROM Employee
GROUP BY Dept_no;

e)  SELECT Dept_no, AVG(salary), COUNT(*) FROM Employee
GROUP BY Dept_no HAVING COUNT(*) » 5;




Q3)

e)  aI†er †abIe Supply add pincode INT
add City varchar(20);

b)  crea†e view supplier as select Id_no, Name from Supply;

c)   aI†er †abIe produc†
add primary key (P_no);

d)  crea†e index index1 on supply (S_code);

e)  alter table emp modify pincode integer(5);

Q4)

o)  select Player.RoII_no, Player.Name, Match.Match_Date from Player, Match
where Player.RoII_no = Matcht.RoII_no:

b)  select Player.RoII_no, Match.Match_no from Player, Match where Player.RoII_no = Match.RoII_no 
and Match.Match_no = 2;

c)  crea†e view info as
select Player.RoII_no, Player.Name, Match.Match_no, Match.Match_Date from Player, Ma†ch
where Player.RoII_no = Match.RoII_no:

d)  select min(RolI_no) from Player;

e)   selec† * from Ma†ch where Ma†ch_da†e in



( select max(Match_date)
From Match ):


Q5)  sELECT *
FROM employees
WHERE salary ZN (SELECT max(salary) FROM employees GROUP BY job_name)
ORDER BY salary DESC;

b)  select length(’ENGINEER’) as len;

c)  select Employee.Employee_name, Department.Dept_Iocation,
Department.Dept_Name from Employee
inner join Department on Employee.Dept_no = Department.Dept_no and salary»1500;

d)  SELECT Dept_no, SUM(salary) FROM employees
GROUP BY Dept_no:


Q6)

o)  selec†Insru†or.I1d,Ins†rucor.I_nOmefomIns†rucMr
innerjoins†uden†onS†udentS_id:Ins†ruc†or.I_id:

b)  select * from Instructor where Instructor.I_id not in
(select Student.S_id from Student);

c)  ALTER TABLE Instructor ADD age ZNT:

d)  select S_name from Student
where S_name like '%ra%’;



Q7
(persons toble) 
crea†e †abIe persons (id in†,
firs†nQme varchar(30), lastname varchar(30), age int ):

insert into persons values (1, "mahesh", "sharma", 30),
(2, "suresh", "varma", 30),
(3, "ramesh", "vishwakarma", 30);

(orders table)
CREATE TABLE Orders (
OrderlD int NOT NULL, OrderNumber in† NOT NULL,
PersonlD int


insert into orders values
(1, 4456, 3),
(2, 5676, 1),
(3, 7687, 2);

(no† null cons†roin†) alter table persons modify age int not null;

(add unique key constrain†)
alter table persons
add constrain† uc_con unique (id):

(drop unique key constraint) alter table persons
drop constrain† uc_con:




(add primary key constraint)
alter table persons
add constrain† pk_con primary key (id)

(drop primary key constraint) alter table persons
drop primary key;

(add foreign key constraint) alter table orders
add constraint fk_con foreign key(PersonZD) references persons(id):

(drop foreign key constraint) alter table orders
drop foreign key fk_con;

(add check constrain†) alter table persons
add constrain† chk_age check (age»=30):

(drop check cons†roin†)
alter table persons
drop constraint chk_age;

(add default constraint)
alter table persons
alter age set default 20;

(drop defaul† constrain†) alter table persons
alter age drop default:


Q8)  create table student (id integer, name varchar(10), marks integer); insert into student (id, 
name, marks)
values
(1, "Rahul", 10),
(2, "Karan", 20),
(3, "Neha", 30),
(4, "Kunal", 40);

select count(*) from student: select sum(marks) from student: select avg(marks) from student: 
select max(marks) from student: select min(marks) from student:

Q9)
(†o odd new column)
ALTER TABLE student ADD L_name varchar(20);

(†o drop column)
ALTER TABLE student DROP column L_name;

(to set the default value to a column) ALTER TABLE student
ALTER address SET DEFAULT 'Mumbai’;

(†o drop †he defoul† volue of o column)
alter table student modify Address char(30);

(to chan9e column name) alter table student
chnnge L_name Last_name varchar(20):

(add constraint)
alter table student
add constrain† con_firs† unique(id);




(drop cons†rain†)
alter table student
drop constrain† con_firs†:


(†o renome column)
alter table student
rename column L_name to Last_name



QIO)

(AND operator) select * from student
where name like '%a%' and marks>10;

(OR operator)
select * from student
where name like '%a%' or marks>10;

(NOT)
select * from student
where not marks>20;

(find name ending with 'r') select * from student where name like '%r';

(find name starting with 'n') select * from student
where name like 'n%':

(find name containing 'a' )
select * from student
where name like '%a%';
